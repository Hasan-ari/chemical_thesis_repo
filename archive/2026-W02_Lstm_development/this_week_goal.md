 DATA PRE-PROCESSING REQUIREMENTSStrict Rule: The time-series data must have a fixed, uniform step size.Action: When generating synthetic data from the ODE solver, strictly use np.linspace(0, 20, 2500). Do not use adaptive stepping.Split: Use an 80/20 Split.Train: First 2000 steps.Test: Last 500 steps.Constraint: The model must NOT see the last 500 steps during the training phase.2. LSTM ARCHITECTUREStrict Rule: The model needs a longer memory window.Action: Set SEQ_LEN = 100.Structure: Stacked LSTM (e.g., 128 units -> 64 units -> Dense Output).Training: Train for at least 500 epochs to ensure convergence with the new sequence length.3. CORE TASK: CHAIN CORRECTION (Recursive Forecast)Strict Rule: Validate the model using a "Free-Running" loop, NOT Teacher Forcing.The Algorithm:Random Start: Pick a random index $t$ from the Test Set (the unseen last 20%).Context: Take the 100 steps prior to $t$ as the initial input.Recursive Loop (150 Steps):Predict step $t+1$.The Chain: Feed this prediction back into the input to predict step $t+2$.Repeat this for 150 steps.Do NOT use any ground truth data during this loop.4. DELIVERABLES (The Proof)The code must output the following specific analytics:Graph (chain_test.png):Plot the Recursive Prediction (Red dashed line) vs Ground Truth (Blue solid line) for the 150-step window.Include variables: nH2_g, nH2S_g, SO4.Divergence Check:Calculate the error at step 50, step 100, and step 150.Report if the error grows exponentially (divergence) or stays bounded (stable).